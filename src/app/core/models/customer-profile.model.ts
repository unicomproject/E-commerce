export interface CustomerProfileResponse {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
}

export interface CustomerProfileUpdateRequest {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
}
